// Ambient declarations for side-effect style imports (e.g. `import "./globals.css"`).
// Next.js supplies these via its generated types, but this guarantees the editor's
// TypeScript server always resolves them even before those types are generated.
declare module "*.css";
declare module "*.scss";
declare module "*.sass";
